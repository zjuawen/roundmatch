<!--index.wxml-->
<view class="container">

  <!-- 用户 openid -->
  <view class="userinfo">
    <block wx:if="{{!hasUserInfo}}">
      <button wx:if="{{canIUseGetUserProfile}}" bindtap="getUserProfile">点击授权允许读取用户头像昵称</button>
    <button wx:else open-type="getUserInfo" bindgetuserinfo="onGetUserInfo" class="userinfo-avatar" style="background-image: url({{avatarUrl}})" size="default">
        <button>按钮</button>
    </button>
      <!-- <button wx:else open-type="getUserInfo" bindgetuserinfo="getUserInfo"> 获取头像昵称 </button> -->
    </block>
    
    <!-- <view class="userinfo-nickname-wrapper">
      <button class="userinfo-nickname" bindtap="onGetUserInfo">点击允许授权读取用户识别号后进入</button>
    </view> -->
  </view>

  <!-- 测试入口 -->
<!--   
  <view class="uploader">
    <navigator url="../testIndex/index" open-type="navigate" class="uploader-text">
      <text>测试用</text>
    </navigator>
  </view>
 -->

</view>
