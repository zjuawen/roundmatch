# 数据库配置
# PostgreSQL 数据库连接配置

# 数据库主机地址
DB_HOST=localhost

# 数据库端口（PostgreSQL 默认端口为 5432）
DB_PORT=5432

# 数据库用户名
DB_USER=roundmatch

# 数据库密码
DB_PASSWORD=roundmatch

# 数据库名称（使用 postgres 数据库）
DB_NAME=postgres

# Schema 名称（使用 roundmatch schema）
DB_SCHEMA=roundmatch

# 环境变量（development, production, test）
NODE_ENV=development

# 服务器端口
PORT=8300

# Session 密钥（生产环境请修改为随机字符串）
SESSION_SECRET=roundmatch token
